import React from 'react';
import {Link} from 'react-router-dom';
import {Container, Media} from 'react-bootstrap';

class DirectMessage extends React.Component{
  render(){
    return(
      <div>Slide in my DM's, baby.</div>
    );
  }
}

export default DirectMessage;